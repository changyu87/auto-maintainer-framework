{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next scheduling housekeep wave's baseline; single-use measured-reduction reference, re-anchored after the issue/PR-named dispatch-description cycle added a spec section.",
  "description": "Doc-surface line counts of scheduling. The prior wave slimmed docs/spec.md to 871 lines; the FT-E loop-redesign reconciliation re-anchored it to 900; the self-healing known-port adapter-map migration re-anchored it to 936; the issue/PR-named dispatch-description cycle (a real behavior cycle) additively re-anchors spec.md to 944, the same way #244 retired the deletion wave's contract-unchanged gate. The housekeep e2e test asserts the current docs are NOT larger than these re-anchored baselines and that the load-bearing tokens survived. The historical pre-wave spec baseline (commit 7a42f9c) was 871; FT-E was 900.",
  "baseline_commit": "dispatch-desc-refs",
  "docs": {
    "docs/spec.md": 944,
    "docs/contract.md": 96
  }
}
