---
feature: scheduling
version: 0.36.0
owner: changyu87
deprecation_criterion: Superseded when scheduling moves to a different clock source (e.g. a native plugin cron API), or when a native rabbit/plugin config system subsumes the wiring-config CLIs.
---

# scheduling — Contract

```json
{
  "provides": {
    "files": [
      "DEFAULT_ROUTE: the shipped read-and-idle spine GUARD->DRAIN->PULL->PERSIST->EXIT as a route.json dict (states/edges/terminal, incl. the DONE/HALTED terminals)",
      "DEFAULT_ADAPTER_MAP: every known port -> 'run_tick:make_<port>' (incl. TRIAGE, PRIORITIZE, IMPLEMENT, resolvable even though the default route omits them)",
      "built-in adapter factories make_guard/make_exit/make_drain/make_persist/make_pull/make_triage/make_prioritize/make_implement — each factory(runtime) -> (StateManifest, run_callable) wrapping the EXISTING sibling adapter unchanged (the adapter-wiring factory convention)",
      "immediate-refire (§3.3.3, POOL-based): _work_remains(work_items, triage_memory, backoff_ledger, threshold, now) is a PURE predicate — it computes candidates = _filter_triage_work_items(work_items, triage_memory) (the SAME §3.5.3 skip-filter TRIAGE uses: drops done/deferred-AND-unchanged, keeps new/changed/active) and returns True iff ANY candidate is BOTH TRIAGE-acceptable (wi.Triage(now).classify(item)[0]=='accepted') AND not blocked-and-unchanged (NOT (backoff[id].blocked_count>=threshold AND item.updated_at NOT strictly after entry.deferred_at_updated_at)); invalid/closed/stale items never count, a blocked item updated past the pin IS actionable. Reusing the skip-filter guarantees no busy-loop. make_exit WRAPS lifecycle-dispositions' Exit: EXIT's manifest reads [tick_outcome, work_items, state_path], writes [tick_outcome], emits the inner refire/idle/break/halt; when the inner outcome is 'empty' AND the route has an ACTING agent-state (effect present, threaded as runtime['has_acting_agent']) AND _work_remains(work_items, persisted_triage_memory(state_path), persisted_backoff_ledger(state_path), threshold, now) is True, it rewrites tick_outcome to 'work-remains' so EXIT selects RUNNING/refire. The OLD committed-work (un-acted/un-handled work_order) gating is REMOVED — the refire keys on the POOL, not on this tick's committed orders. It overrides ONLY the 'empty' outcome (restart/fault delegated unchanged). work_items is in the data-readiness initial set. The read-and-idle DEFAULT route + any empty-pool/all-done pool tick still IDLE (no acting agent-state -> override never fires)",
      "close-the-loop adapter factories make_verify/make_review/make_integrate/make_cleanup (verify-integrate) — each factory(runtime) -> (StateManifest, run_callable) wrapping the EXISTING verify-integrate state unchanged, so the full act route '… IMPLEMENT -> VERIFY -> REVIEW -> INTEGRATE -> CLEANUP -> PERSIST -> EXIT' wires by a pure route.json edit (all ports pre-mapped). make_verify wraps vi.Verify (the READ-ONLY PR gate, source=vi.gh_open_pr_source, reads cross_cutting_risk, writes verdicts + cross_check, FT-B/D §3.7.6); make_integrate binds the loaded governance mode so INTEGRATE — a THIN merge reading ONLY verdicts (FT-C/D) — merges (vi.gh_pr_merge_sink) each ok verdict's PR only at auto-merge WITHOUT any review-approval read, consuming sg.permits + vi.merge_guardrails, the would-merge plan logged at propose; make_cleanup wraps vi.Cleanup (v1-thin branch/release hygiene). make_review is the ADVISORY quality state between VERIFY and INTEGRATE (FT-C, DESIGN §3.7.7): a NON-ACTING AGENT-state — the real reviewer is the auto-maintainer-reviewer subagent wired by setting the REVIEW port to AGENT_PORT_TEMPLATES['REVIEW']; this deterministic factory is the resolvable DEFAULT that reads verdicts and writes an EMPTY review_findings list (signal OK — REVIEW is advisory, so it ALWAYS continues to INTEGRATE; AGENT_PORT_TEMPLATES['REVIEW']['signal_rule']='always_ok' and the no-op _run emits OK, not EMPTY). REVIEW is NO LONGER a merge gate — a lazy reviewer costs only missed advisory notes, never an unsafe merge. REVIEW's declared emits stay [OK, EMPTY] (verify-integrate REVIEW_MANIFEST, UNCHANGED) so a seeded route carrying a REVIEW--EMPTY-->PERSIST edge still validates; that EMPTY edge is valid-but-dead (never taken with always_ok)",
      "verify-integrate close-the-loop read products (per-tick #64 ephemeral, persisted under VERDICTS_KEY='verdicts' / REVIEW_FINDINGS_KEY='review_findings' / INTEGRATION_RESULT_KEY='integration_result'; each empty when the route omits its producer, never a stale carry-forward): VERIFY writes verdicts (a list of Verdict dicts) + cross_check, REVIEW writes review_findings (a list of DiscoveredIssue-conforming records, FT-C), INTEGRATE writes integration_result (an object). persisted_review_findings(state_path) -> [] reads the #64 review_findings snapshot; _seed_context registers + seeds cross_cutting_risk empty (no-risk default) when TRIAGE OR VERIFY is routed, cross_check when VERIFY is routed, verdicts when VERIFY is routed, review_findings when REVIEW is routed, and integration_result when INTEGRATE is routed (INTEGRATE reads ONLY verdicts — the review_verdicts coupling is retired). At the terminal the review_findings slot is an ADDITIONAL discoveries source into _flush_report/_gather_discoveries, filed via wi.file_discoveries on the SAME journaled-idempotency + dedup-vs-open (known_open=work_items) path as handoffs[].discovered_work",
      "persisted-read-product helpers persisted_work_items/_count, persisted_work_orders/_count, persisted_execution_plan/_count, persisted_handoffs/_count — read the per-tick ephemeral read products from durable state for status reporting",
      "route_source(project_dir=None) -> (label, path): the single source of truth for the loaded route's SOURCE — ('override', '<project_dir>/.auto-maintainer/route.json') when that file exists (the SAME path adapter-wiring loads), else ('default', None); route_source_label(...) renders it as the trace/status token 'default' or 'override:<abs path>' (#59)",
      "BUDGET_KEY = 'budget': the durable-state key under which the cross-tick budget window {window_key, spent_tokens} is persisted (a durable cross-tick fact like the counter, NOT a per-tick ephemeral read product)",
      "persisted_budget_state(state_path) -> {window_key, spent_tokens}: reads the durable budget window from durable state (default {} when never written), for status reporting",
      "governance threading: run_tick loads sg.load_config(project_dir) (alias load_governance) once per tick and threads it into the factory runtime dict under a 'governance' key (so acting adapters can consult permits/budget, and run_tick reads config.backoff.threshold for the backoff gate); the existing runtime keys project_dir/runtime_dir/source/now are preserved",
      "TICK_CHECKPOINT_KEY = 'tick_checkpoint': the durable-state key under which the agent yield/resume checkpoint is persisted while a tick is PAUSED at an agent-state ({next_state, tick_id (the per-tick id #112, read back on resume so the step->resume pair shares one id), slots, path, signals, output_dir, pending:{state, writes, signal_rule, cardinality, dispatches:[{output_path, schema}...]}}); the SOLE source of truth for the paused dispatch (crash-safety). output_dir + the per-dispatch output_path are persisted so a crash-safety re-emit produces the byte-identical output_path and resume knows which files to read. Cleared on reaching the terminal",
      "persisted_tick_checkpoint(state_path) -> {} | checkpoint: reads the durable PAUSED checkpoint (default {} when no tick is paused)",
      "run_tick agent yield/resume contract (DESIGN §2.8, §3.4.6 file-based context isolation): when the resolved route contains >=1 agent-state, run_tick resolves output_dir=${runtime_dir}/dispatch-out/, builds envelopes via ad.build_envelopes(entry, slot_values, {tick_id, mode}, state, output_dir) (each envelope's output_contract{slot, schema, output_path} and its rendered ## Handoff name the output_path where the subagent WRITES its JSON), DELETES any pre-existing file at each output_path (so a stale prior-tick file can't be misread), PAUSES, and returns a PAUSED dict {status:'paused', state:<name>, dispatches:[{subagent_type, prompt_path (an ABSOLUTE file under output_dir holding the rendered envelope ad.render(env), named parallel to output_path: '<state>-<di>-<ii>.json' -> '<state>-<di>-<ii>.prompt.md'; the rendered envelope is delivered by FILE REFERENCE, NOT inline, so the orchestrator never receives/holds the rendered envelope — symmetric with the file-based OUTPUT; written at the PAUSE where stale OUTPUT files are cleared, overwritten each emit, byte-identical on a crash-safety re-emit via the checkpoint round-trip), writes, output_path, signal_rule, cardinality, isolation (the dispatch entry's isolation, e.g. 'worktree', or null when absent), description (the dispatch entry's description else a default f'{state} dispatch' / f'{state}: {item}'), item?}...]} after durably checkpointing (it NEVER calls the Agent tool). isolation + description + prompt_path let the executor call Agent(subagent_type, description=..., prompt=<a SHORT reference telling the subagent its envelope is the file at prompt_path, to read it IN FULL and follow it literally>, isolation=...) — the orchestrator does NOT open/read prompt_path; the SUBAGENT reads it. run_tick(resume=True) READS each pending dispatch's output_path FILE, validates it via ad.validate_output(content, schema), applies + persists the collected slot value, and continues to the next pause or terminal; a MISSING output file or an invalid file returns {status:'invalid_output', state:<name>, reason:<str> (naming the missing path)} with the checkpoint left intact (re-dispatchable; never a crash). The invalid_output re-emit is PER-DISPATCH: only the dispatches whose output_path file is missing or invalid are re-dispatched; a dispatch whose output_path already holds a schema-valid output is PRESERVED (its file left on disk, NOT stale-deleted, NOT re-run) — this prevents re-running an IMPLEMENT dispatch that already opened a PR into a DUPLICATE PR (the live duplicate-PR root cause). When every dispatch's output is finally present+valid, ALL of them (preserved-valid + freshly re-run) are collect_outputs-assembled into the slot as before. There is NO orchestrator-marshalled blob / resume_dispatch list — the subagent-written output files are the sole resume input. A fresh run_tick with no resume that finds an existing checkpoint re-emits the PAUSED dispatch (byte-identical output_path, crash-safety, idempotent), subject to the SAME per-dispatch narrowing (already-valid outputs preserved, only not-yet-valid dispatches re-emitted). A pure-script route is UNCHANGED (runs via tick_orchestrator.run, returns the disposition signal string)",
      "run_tick trust-gate for ACTING agent-states (DESIGN §2.3 / §3.8.2 trust ladder): an ACTING agent-state is an agent-adapter whose dispatch entry carries a truthy `effect` string (safety-governance's closed effect set {implement, open_pr, merge, file}). Before pausing at an acting agent-state run_tick computes permitted = sg.permits(effect, mode) (mode from the loaded governance) — the deterministic lib's decision, never the model's. NOT permitted (e.g. dry-run, where permits is False for every effect): run_tick does NOT pause/dispatch — it builds the per-dispatch items via ad.build_envelopes(...) and synthesizes one INERT planned handoff per item {work_order_id:<id or null>, status:'planned', artifact:{kind:'none', ref:null}, discovered_work:[], blocked_reason:null}, ad.collect_outputs them into the writes slot, writes the slot, ad.compute_signal the route signal, persists the read product, emits state_run/signal events (state_run detail notes gated=dry-run), and CONTINUES the driver (no PAUSE, no checkpoint, no spend, no subagent). Permitted (propose / auto-merge): the normal PAUSE-for-dispatch path runs unchanged so the executor dispatches the real subagent. A NON-acting agent-state (no `effect`, e.g. a TRIAGE adapter) is UNCHANGED — the trust-gate does not apply and it always pauses. Read products stay #64 per-tick ephemeral; #123 budget persistence + #109 journal-free checkpoint unchanged",
      "ACTED_LEDGER_KEY = 'acted_ledger' + persisted_acted_ledger(state_path) -> {work_order_id: {outcome, ref, acted_at_updated_at}}: the durable cross-tick acted-ledger (a durable cross-tick fact like BUDGET_KEY, NOT a #64 read product; default {} when never written) recording which work orders an ACTING agent-state already acted on, for idempotency (§3.2.4); an 'opened' entry also stores acted_at_updated_at (the source issue's updated_at at act time) for the acted-ledger re-entry rule (§3.8.5-symmetric, #204)",
      "gh_pr_state_source(pr_ref, repo=None, runner=subprocess.run) + DEFAULT_PR_STATE_SOURCE: the injectable PR-state seam for the acted-ledger re-entry check (mirrors verify_integrate.gh_open_pr_source). Shells `gh pr view <pr_ref> --json state,mergedAt` and returns {state: 'OPEN'|'CLOSED'|'MERGED', merged: bool}; the subprocess runner is injectable for deterministic, network-free unit tests. run_tick(pr_state_source=...) overrides it",
      "run_tick doer governance for ACTING agent-states on the PERMITTED (dispatch) path (sg.permits(effect, mode) True): (1) acted-ledger idempotency — when building the per_item dispatch set run_tick FILTERS OUT any work_order_id already in persisted_acted_ledger(state_path) (already acted — never re-dispatch / no second PR); if after filtering NO items remain the state does NOT pause (synthesizes an inert result, computes the signal, continues); on resume run_tick RECORDS each newly-acted handoff into the ledger (ledger[work_order_id] = {outcome: handoff['status'], ref: handoff.get('artifact',{}).get('ref'), acted_at_updated_at: <the source issue's current updated_at, resolved via the dispatched work_orders' work_order_id->work_item_id map>}) via load-modify-save of ONLY ACTED_LEDGER_KEY (preserving every other durable key). ACTED-LEDGER RE-ENTRY (§3.8.5-symmetric leak fix, #204): an already-'opened' work order RE-ENTERS the per_item dispatch set (its acted-ledger entry CLEARED, the item re-dispatched) when BOTH (a) the source issue's current updated_at has ADVANCED past acted_at_updated_at AND (b) its recorded PR ref is CLOSED-AND-NOT-MERGED (queried via the injectable pr_state_source ONLY for entries whose updated_at advanced — bounds the gh calls to changed issues). It stays LOCKED otherwise: merged (done), still-open PR (pending review), or closed-but-issue-unchanged (the human closed it without a redo — no thrash); a raising/malformed pr_state_source stays locked + never crashes the tick. Net: close an auto-PR + update the issue -> the loop re-attempts; close it + touch nothing -> the loop leaves it alone (removes the manual durable-state ledger surgery). (2) budget pre-gate — BEFORE pausing, run_tick evaluates the budget window (sg.evaluate_budget(gov, persisted_budget_state(...), budget_clock)); if allowed is False (per_day exhausted) it does NOT pause/dispatch — it synthesizes a DEFERRED result (handoffs status:'blocked', blocked_reason naming the budget exhaustion, for the not-yet-acted items), computes the signal, continues — NO spend, NO dispatch; the items stay un-acted (NOT in the ledger) so they retry next window. (3) spend metering on resume — run_tick(resume=True, spent=<int>) (default 0, back-compatible) calls sg.record_spend(budget_state, budget_clock, spent) into the durable budget window and persists it after applying the subagent outputs. ALL THREE apply ONLY to acting agent-states; a NON-acting agent-state (TRIAGE, no effect) is NOT budget-pre-gated and NOT ledger-filtered (unchanged — always dispatches), the dry-run inert path + pure-script routes are byte-identical. The acted-ledger + budget window are durable cross-tick facts (load-modify-save preserving other keys), never #64-ephemeral",
      "structured event log: run_tick emits a per-tick structured event log to ${runtime_dir}/events.jsonl, consuming the observability lib UNCHANGED (observability.EventLog.append + the closed EVENT_KINDS vocabulary). Each tick appends, in order: tick_start (detail carries route source + mode) at a FRESH tick start; state_run + signal per visited non-terminal state (pure-script path derives them from RunResult.path/RunResult.signals; agent-driver path emits inline as each SCRIPT state runs); pause + dispatch (subagent_type + writes in detail) when pausing at an agent-state; resume on a --resume invocation; disposition (the resulting disposition + EXIT signal); tick_end (final signal + the four read-product counts work_items/work_orders/execution_plan/handoffs, the REPORT counts reported_filed/reported_skipped/reported_errored, the INTEGRATE merge results merged (count) / integrate_skipped (count) / integrate_errored (count) / auto_merge_enabled (count of PRs on which INTEGRATE enabled GitHub native auto-merge this tick — a PENDING success, distinct from integrate_errored) / merged_refs (list of merged pr_refs, from the integration_result read product) / deduped (count = len(reconcile_result.deduped), the same-issue duplicate loop PRs RECONCILE closed this tick, from the reconcile_result read product; 0 when no RECONCILE state or reconcile_result absent), and a refire boolean disambiguating idle-because-no-work (false) from refire-because-work-remains (true) — all in detail; the one-line trace also gains a compact merged=<n> token plus integrate_errored=<n>, auto_merge_enabled=<n>, and deduped=<n> each only when >0). tick_end.detail ALSO carries additive per-issue/per-PR IDENTIFIER lists (so the log alone answers which issues + which PRs): handoffs_detail [{work_order_id, status, ref}] from persisted_handoffs (ref = opened PR artifact.ref else null); integrated {merged:[{pr_ref,url}], skipped:[{pr_ref,reason}], errored:[{pr_ref,reason}]} from integration_result; reported_detail [{dedup_key, tracker_ref, url}] for discoveries filed this tick (new report_ledger entries). All additive — existing counts + merged_refs + trace unchanged; empty lists when a route produces none; observability EVENT_KINDS envelope unchanged (detail stays an opaque dict). The event ts reuses the tick's already-resolved tz-aware budget now (the injected now; never an implicit wall clock), so the log is DETERMINISTIC; seq is monotonic across a multi-invocation agent tick (observability assigns it via the file's line count). Every event carries a per-tick tick_id that discriminates ticks (#112): a durable monotonic counter (TICK_ID_COUNTER_KEY, SEPARATE from the work counter — a read-only route never bumps the work counter, so it used to collapse every read-only tick to tick_id=0) assigned ONCE at FRESH tick start (incrementing the counter; first tick is 1, never 0) and carried through the tick via the durable checkpoint — stamped into TICK_CHECKPOINT_KEY at a PAUSE and READ BACK on a --resume / crash-safety re-emit. It is therefore (1) DISTINCT across ticks AND (2) STABLE across a single tick's --step -> --resume on ALL routes (incl. the agent route, whose --step/--resume are separate processes injecting no now), and is deliberately NOT derived from the wall clock/now. The SAME tick_id seeds the rendered agent-dispatch {tick_id} slot. Event emission is purely ADDITIVE — it changes NO existing behaviour (the trace, signals, disposition, slot persistence, #64 ephemerality, the budget window all unchanged); run_tick emits no kind outside EVENT_KINDS",
      "RECONCILE state wiring (Wave-2 consumer): make_reconcile(runtime) -> (vi.RECONCILE_MANIFEST, run) is a built-in factory wrapping verify_integrate.Reconcile (mirroring make_integrate over Integrate), registered at 'run_tick:make_reconcile' in DEFAULT_ADAPTER_MAP; RECONCILE runs BEFORE PULL (route GUARD->DRAIN->RECONCILE->PULL->...). It binds the loaded governance mode (the mutating tiers run only at permits('merge', mode)) and injects the production seams at factory-call time (vi.gh_pr_state_source / gh_issue_state_source / gh_issue_close_sink / gh_pr_close_sink / gh_issue_comment_sink / reconcile_rebase_worktree) + the resolved default branch. The bound run SEEDS Reconcile's acted_ledger slot from the durable ACTED_LEDGER_KEY 'opened' entries (via _reconcile_ledger_seed(ledger) -> [{work_order_id, pr_ref, issue_ref, repo}], deriving issue_ref from the wo- prefixed work_order_id and repo from its owner/repo portion), delegates to Reconcile.run, then PERSISTS the outcome via _persist_reconcile_outcome(state_path, seed, recon): each reconcile_result.closed_issues entry STAMPS its acted-ledger entry outcome='closed' (idempotency — a later opened-only seed excludes it, never re-comments) and each relanded entry CLEARS its ledger entry so the §3.8.5 re-entry gate re-lands it next tick (load-modify-save of ONLY ACTED_LEDGER_KEY). _seed_context registers + seeds acted_ledger (empty []) + reconcile_result (empty ReconcileResult) ONLY when RECONCILE is routed; acted_ledger is in the data-readiness initial set. RECONCILE is ADVISORY (emits only OK) — a single-entry fault is recorded under errors and never blocks the tick; tests inject fakes for every seam + a fake durable state (no network)",
      "reject-disposition enactment at TRIAGE (Wave-2 consumer): at the TRIAGE resume (the non-acting agent-state writing the work_orders slot), after the triager writes work_orders, run_tick calls _enact_rejects(state_path, work_orders, work_items, mode, reject_sink): for every wi.reject_dispositions(work_orders) order it invokes reject_sink(issue_ref, repo, reason) (DEFAULT_REJECT_SINK = wi.gh_issue_reject_sink — comment carrying the reason + apply wi.REJECTED_LABEL, NEVER close; idempotent) and records triage_memory[work_item_id] = {updated_at, status: 'rejected'} (load-modify-save of ONLY TRIAGE_MEMORY_KEY). Trust-gated by sg.permits('file', mode) (same rung as the REPORT flush): at dry-run the intent is logged, not written (no sink call, no memory record). _TRIAGE_SKIP_STATUSES now includes 'rejected', so a rejected-AND-unchanged item is filtered from the next TRIAGE dispatch (_filter_triage_work_items) while a CHANGED (advanced updated_at) item re-admits. run_tick(reject_sink=...) overrides the sink for network-free tests",
      "TICK_ID_COUNTER_KEY = 'tick_id_counter' + persisted_tick_id_counter(state_path) -> int: the durable per-tick id counter (a durable cross-tick fact like the work counter, NOT a #64 read product; default 0 when never written, so the first FRESH tick assigns id 1). SEPARATE from the work counter. _assign_tick_id(state_path, resume, checkpoint) resolves THIS invocation's tick_id: read back checkpoint['tick_id'] on a resume / re-emit (legacy checkpoints without the field fall back to the counter), else mint counter+1 at a FRESH tick (load-modify-save ONLY TICK_ID_COUNTER_KEY)",
      "release-needed detection (#319, the human-release-owed signal — the auto-maintainer is NOT self-deployable, so it performs NO build/commit/push/regenerate): _release_needed(integration_result, project_dir, files_source, repo) -> bool is a PURE detector. It returns True iff (a) _self_deploy_repo_root(project_dir) resolves (the maintained project IS the framework's own checkout, detected by walking up for build_plugin.SELF_DEPLOY_MARKER) AND (b) >=1 PR in integration_result.merged has a diff touching SHIPPED source (build_plugin.touches_shipped_src over the injectable files_source's per-PR changed files). It fires on the merged shipped-src change ALONE — no governance / self_deploy knob / package_deployed param (nothing self-deploys). False (no query) when build_plugin is not importable (a normal install) or the project is not the framework checkout (keeps the gh calls off non-self-deploy projects); a docs/test-only merge and a no-merge tick are False too. gh_pr_files_source(pr_ref, repo=None, runner=subprocess.run) + DEFAULT_PR_FILES_SOURCE is the injectable per-PR changed-files seam (shells `gh pr view <pr_ref> --json files`, runner injectable for network-free tests; run_tick(pr_files_source=...) overrides it). build_plugin is imported ONLY for bp.touches_shipped_src + bp.SELF_DEPLOY_MARKER. At the terminal (clean done path, signal != halt) run_tick calls _release_needed and surfaces it: a standalone `release_needed` token rides the one-line trace when True, and the tick_end event detail carries release_needed:<bool>. No self-deploy / build / commit / push / version-bump field rides the trace or event",
      "JSON tick CLI run_tick.main(argv) (also the __main__ entrypoint): a THIN deterministic wrapper around the EXISTING run_tick structured returns (no new tick logic) so the later executor skill can drive the yield/resume loop. Bare invocation (no --step/--resume) is UNCHANGED — calls run_tick() and prints the one-line HUMAN trace (existing bash callers keep working). --step runs to the next pause/terminal and prints a SINGLE JSON object to stdout: done -> {status:'done', signal:'<idle|halt|...>', trace:'<one-line trace>'}; paused -> {status:'paused', state:<name>, dispatches:[{subagent_type, prompt_path (the rendered envelope delivered by FILE REFERENCE, NOT inline, so --step stdout stays small/un-truncated), writes, output_path, signal_rule, cardinality, item?}...]}; invalid_output -> {status:'invalid_output', state:..., reason:...}. --resume takes NO file argument: it calls run_tick(resume=True), which reads each paused dispatch's subagent-WRITTEN output FILE at the checkpoint's output_path, and prints the same envelope shape. --resume gains an optional --spent <int> (default 0) that meters that spend into the durable budget window via sg.record_spend on a resume of an acting agent-state. In --step/--resume mode stdout is PURE JSON (the skill parses stdout) — the human trace is captured into the JSON `trace` field, never leaked raw. Exit codes: done/paused -> 0; invalid_output (bad agent output OR missing output file) -> 1 (no crash). The path flags --runtime-dir/--state/--journal/--project-dir point the CLI at a temp runtime for tests; omitted -> production defaults (resolve_runtime_paths). The PULL source is not a CLI flag (defaults to DEFAULT_PULL_SOURCE / live gh); tests stub it by overriding DEFAULT_PULL_SOURCE"
    ],
    "scripts": [
      "src/run_tick.py: deterministic single-tick runner — resolves runtime, loads config via sg.load_config(project_dir) (alias load_governance) and threads it into the runtime dict, calls adapter_wiring.build_loop(DEFAULT_ROUTE, DEFAULT_ADAPTER_MAP, runtime, start='GUARD', initial=[...]) to load (project-local override else default) -> resolve -> validate -> (route, states), runs tick_orchestrator.run(...), evaluates + persists the durable cross-tick budget window via sg.evaluate_budget(gov, prior_budget_state, now, tick_spend), prints a tick trace (incl. route source #59, plus mode + a compact budget=<spent>/<ceiling-or-none> win=<window_key> field and a budget_paused=<reason> indicator when blocked), persists the per-tick ephemeral read products work_items + work_orders + execution_plan + handoffs (each when the active route produced it, else empty) + the EXIT disposition signal (one invocation = one tick). run_tick(...) accepts injectable now (tz-aware budget clock; defaults to the host local-aware now) + tick_spend (default 0; no model spender yet, tests inject it) + spent (default 0; the spend metered on a resume of an acting agent-state — sg.record_spend into the durable budget window) + resume (default False; the executor sets it after the subagent has WRITTEN its output files at the checkpoint's output_paths). When the route contains agent-states run_tick runs the pausable driver (yield/resume seam) instead of tick_orchestrator.run, resolves output_dir=${runtime_dir}/dispatch-out/, builds file-based dispatch envelopes (deleting any stale output file at pause), checkpoints under TICK_CHECKPOINT_KEY, and returns the PAUSED/invalid_output dict; on resume it reads the subagent-written output files (no marshalled blob); the budget readiness gate is evaluated at FRESH tick start only, not on resume. The durable budget window is persisted on the PAUSE / invalid_output early-return path too (load-modify-save ONLY BUDGET_KEY, preserving the checkpoint + read products), so an agent-route tick's rolled window survives the pause (auto-maintainer-framework#123); on resume the evaluated window is carried forward (new_budget_state = budget['budget_state']) so a {} persisted value resolves to a real {window_key, spent_tokens} and /status shows win=<date> not empty",
      "src/start.py (v0.5.0) / src/stop.py (v0.2.0) / src/status.py: deterministic control scripts (script-tier) owning all state operations; start.py gains a --clear-only mode that performs ONLY the disposition decision (clear a latched STOPPED -> IDLE, REFUSE on ABORTED via non-zero exit, no-op on RUNNING/IDLE/absent) and does NOT run tick #1 — separating the FRESH-start latch-clear from tick #1 so tick #1 of an AGENT route goes through the executor skill (DESIGN §2.8), not start.py's in-process run_tick; the clear-or-refuse decision lives in one place shared by both --clear-only and the default clear+tick mode. start.py ALSO records the durable loop-intent (hb.record_loop_intent) after the latch-clear/refuse succeeds in BOTH modes (a refused ABORTED records nothing) so a future session's SessionStart hook auto-resumes the heartbeat (#31); it deliberately does NOT clear the resume-dedup (heartbeat.py owns that on the SessionStart path — clearing it here would let a 2nd SessionStart in the SAME session re-arm a DUPLICATE heartbeat). stop.py ALSO clears the durable loop-intent AND the resume-dedup (hb.clear_loop_intent) so a stopped loop does NOT auto-resume and a /start in the same session can re-arm. status.py reports disposition + the four read-product counts work_items/work_orders/execution_plan/handoffs (always reported, including 0, matching the tick trace, #69) + the route source (default vs override:<path>, #59) via run_tick.route_source + the governance mode + the compact budget field (budget=<spent>/<ceiling-or-none> win=<window_key>) + a budget_paused=<reason> indicator when the durable budget is exhausted. status.py ALSO provides status_data() -> dict (machine-first: plugin_version + disposition/awaiting/mode/budget/read-product counts/reported/route{source,states,chain}/runtime_dir) and render_status(data) -> the derived human view (philosophy §1); the CLI prints render_status by default, --json prints status_data(), --line prints the legacy status_line() (retained byte-identical). plugin_version is read from <lib_dir>/../.claude-plugin/plugin.json (installed-plugin context; null when absent). The active route (states + happy-path chain) is resolved via run_tick's shared route-resolution helper (shipped default-config/route.json fresh else embedded DEFAULT_ROUTE, then project override) so status never diverges from what build_loop runs; listed even when default",
      "src/heartbeat.py (v0.1.0): the durable loop-intent + cross-session auto-resume decision lib (#31, DESIGN §3.3.2). Owns two runtime-dir markers ALONGSIDE the lifecycle disposition/lock markers (it only READS the disposition, never edits it): loop-intent (record_loop_intent/clear_loop_intent/read_loop_intent/loop_intent_is_running; INTENT_RUNNING='running'; set running by /start, cleared by /stop) and last-resume-session (read_resume_dedup/mark_resumed/clear_resume_dedup; the cross-session arm-once dedup). The pure decision should_auto_resume(runtime_dir, session_id) -> True only when (1) loop-intent is running, (2) the disposition is NOT STOPPED/ABORTED/RESTART_NEEDED (a latched/restart-owed loop is NEVER silently re-armed — RESTART_NEEDED is BLOCKED here, NOT the DESIGN §3.3.4 resume-DRIVE flow, which stays deferred), and (3) this session has not already armed (read_resume_dedup != session_id). Pure function of on-disk state + session id (deterministic, unit-testable, no session/clock/scheduler). All marker writes are atomic (temp + os.replace). record_loop_intent does NOT touch the resume-dedup (dedup ownership is the SessionStart path: mark_resumed + the /stop clear); clear_loop_intent clears BOTH intent and dedup (a stop ends the arming epoch). scheduling consumes lifecycle-dispositions UNCHANGED — heartbeat.py reads ld.read_disposition via the public API only",
      "src/route_config.py + src/adapter_map_config.py: deterministic guided wiring-config editors (script-tier). route_config.py load-modify-VALIDATE-saves ${project_dir}/.auto-maintainer/route.json (--show with route_source #59; insert/remove state + add/remove edge ops; each edit validated via adapter_wiring.build_loop over the edited route + active map BEFORE write — a failing edit is rejected, file NOT written; --describe emits the states/edges catalog). adapter_map_config.py load-modify-VALIDATE-saves adapter-map.json (--show; set a port to a script address OR an agent entry — for a KNOWN agent-capable port the user supplies ONLY subagent_type and the CLI fills writes/cardinality/effect/output_example from scheduling's AGENT_PORT_TEMPLATES; validated via adapter_wiring.resolve_states/build_loop before write). AGENT_PORT_TEMPLATES (scheduling-owned) maps each known agent-capable port (TRIAGE, IMPLEMENT, and REVIEW — the ADVISORY REVIEW state wired to subagent_type=auto-maintainer-reviewer, writing review_findings) -> {writes, cardinality, effect?, output_example (a CONCRETE example value, never a schema descriptor)} built from the ports' slot owners (work-intake WORK_ORDERS_SLOT, implement HANDOFFS_SLOT, verify-integrate REVIEW_FINDINGS_SLOT, ...). adapter_map_config.py ALSO provides migrate_known_port_entries(adapter_map) -> adapter_map: a PURE dict->dict self-healing transform that, for each (port, entry) where ad.is_agent_entry(entry) AND port in AGENT_PORT_TEMPLATES, REBUILDS the entry via _build_agent_entry(port, <entry's existing dispatch[0].subagent_type>) — re-deriving writes/cardinality/output_example/inputs/manifest/signal/effect/isolation from the LIVE template while preserving ONLY the subagent_type — so a stale persisted known-port entry (e.g. a v0.6.0 REVIEW writing the retired review_verdicts) self-heals to the current template (review_findings). Script (string) entries, custom-port agent entries (port not in templates), and non-agent entries are left UNCHANGED; it returns a NEW dict (never mutates the input) and is idempotent. run_tick passes it as the migrate= hook of its single aw.build_loop(...) call (LAZILY importing adapter_map_config at the call site to avoid the adapter_map_config->run_tick circular import), so the heal runs on the loaded map BEFORE resolve+validate on every tick"
    ],
    "skills": [
      "ship/skills/start/SKILL.md (/auto-maintainer:start): clears the FRESH-start latch via start.py --clear-only (clear STOPPED->IDLE, or REFUSE on ABORTED and stop), runs tick #1 THROUGH the /auto-maintainer:tick executor (NOT start.py's in-process run_tick, so AGENT-route agent-states are dispatched — DESIGN §2.8), then schedules a recurring PROMPT heartbeat firing /auto-maintainer:tick each interval (NOT a bare run_tick.py command, which cannot dispatch agent-states); latch cleared once at start, not re-cleared per heartbeat. /start OWNS the drain-loop: since /tick runs exactly one tick and stops, /start (both tick #1 and the recurring heartbeat prompt) fires /auto-maintainer:tick AGAIN immediately whenever the completed tick's signal is refire (actionable work remains), repeating until a non-refire signal, so the backlog drains without waiting for the next interval. The interval is config-driven: start.py emits config.heartbeat.interval_minutes (default 3) and the skill schedules at that cadence (#17 resolved). start.py ALSO records the durable loop-intent so the heartbeat is DURABLE across sessions (#31): the SessionStart auto-resume hook re-arms it next session (the skill documents this; no extra skill step needed)",
      "ship/skills/stop/SKILL.md (/auto-maintainer:stop, v0.2.0): invokes stop.py (latch STOPPED + clear the durable loop-intent so the next session's SessionStart hook does NOT auto-resume) and cancels the in-session heartbeat (three durable safeguards: the STOPPED latch, the heartbeat cancellation, and the cleared loop-intent)",
      "ship/skills/status/SKILL.md (/auto-maintainer:status): invokes status.py (default human view) and reproduces its FULL rendered human view VERBATIM in a fenced code block in the reply (NOT a one-line prose summary) — emphasized plugin version, disposition/awaiting/mode/budget/reported/read-product counts, and the active route listing (states + happy-path chain, shown even when default); surfacing the multi-line view in the assistant message avoids Claude Code's long-Bash-output fold",
      "src/clobber.py: deterministic reset script (script-tier). Resolves the runtime dir via run_tick.resolve_runtime_paths and DELETES only runtime-STATE artifacts (durable-state.json, disposition, lock.json, events.jsonl, tick-journal.jsonl, dispatch-out/, heartbeat markers loop-intent + last-resume-session) while PRESERVING user config (config.json, route.json, adapter-map.json, and *.bak/*.migrated backups). Idempotent (missing artifact = action 'absent'), never creates the runtime dir, prints a machine-first payload {mode:'preview'|'applied', artifacts:[{name,path,exists,action:'would-remove'|'removed'|'absent'}], preserved:[names], loop_intent_present:<bool>}. The loop_intent_present flag (heartbeat's durable loop-intent marker, set only by /start) is the deterministic signal the clobber SKILL keys its '/stop first' recommendation on — NOT disposition==RUNNING (which merely means a tick is mid-flight, incl. a paused single /tick). PREVIEW by default (deletes nothing); the internal --apply flag actually deletes (the retired user-facing --yes flag is REPLACED). The verbatim-'yes' confirmation is a SKILL-owned conversational gate, not a CLI flag",
      "ship/skills/clobber/SKILL.md (/auto-maintainer:clobber): DEFAULT = invoke clobber.py (preview, no flag; deletes nothing), render the WOULD-DELETE / PRESERVED tables, key the /stop-first advice on loop_intent_present, then gate on the user typing the verbatim word 'yes' before invoking clobber.py --apply (renders the DELETED / PRESERVED tables); --no-dry-run = invoke clobber.py --apply immediately (no gate). Dispatches NO subagent",
      "ship/skills/tick/SKILL.md (/auto-maintainer:tick): the executor skill — drives run_tick.py --step/--resume and presses the Agent button at agent-states (dispatches the runner's named subagent(s) with the rendered prompt verbatim) until the tick reaches its TERMINAL, then STOPS. It runs EXACTLY ONE tick (one route pass) and does NOT loop on refire: a `refire` final signal is REPORTED (work remains) but the executor never auto-starts another tick — one /tick = one tick even when the issue pool is not drained (the drain-loop is /start's responsibility). all tick logic stays in run_tick.py (the skill only relays dispatch requests). #100 FULLY CLOSED — no orchestrator content-marshalling: each dispatched subagent WRITES its own output to the file named in the rendered prompt's ## Handoff, and run_tick.py --resume (taking NO file argument) reads those subagent-written files itself from the checkpoint. The skill never writes the subagent output, never names a dispatch-result.json, and never hand-rolls serialization — keeping the executor's context clean regardless of output size",
      "ship/skills/route/SKILL.md (/auto-maintainer:route): a guided route.json editor — recommends keeping DEFAULT_ROUTE; on a user edit it walks the change and calls route_config.py to validate (adapter_wiring.build_loop) + write; dispatches NO subagent",
      "ship/skills/adapter-map/SKILL.md (/auto-maintainer:adapter-map): a guided adapter-map.json editor — recommends DEFAULT_ADAPTER_MAP; to wire an agent the user gives subagent_type (+ port) and the skill calls adapter_map_config.py (fills the entry from AGENT_PORT_TEMPLATES for a known port, validates, writes); dispatches NO subagent"
    ],
    "agents": [
      "ship/agents/auto-maintainer-echo.md (auto-maintainer-echo, v2.0.0): the domain-free PROOF echo agent — INTERFACE-PROTOCOL-FREE (DESIGN §3.4.6): its .md is role-only (frontmatter tools:[Write]) and bakes in NO schema, NO output path, and NO output format; the rendered prompt is the complete handoff contract (embedded schema + output_path + ack). Dispatched by subagent_type at an agent-state, for each input item it produces one accepted output and WRITES it to the file named in the prompt's ## Handoff, replying with only a short ack. The echo-TRIAGE adapter-map entry (kind=agent, reads work_items, writes work_orders, dispatch subagent_type=auto-maintainer-echo cardinality once, signal nonempty_else_empty) is valid drop-in config: adapter_wiring.build_loop ACCEPTS it (TRIAGE resolves to an AgentState) and run_tick runs it end-to-end via the yield/resume seam (the subagent writes its work_orders to the dispatch output_path; resume reads the file)"
    ],
    "hooks": [
      "ship/hooks/session-start-resume.py (v0.1.0): the SessionStart auto-resume hook for the durable heartbeat (#31, DESIGN §3.3.2). On SessionStart it resolves the project runtime dir (CLAUDE_PROJECT_DIR else cwd, .auto-maintainer/), reads the session_id from the event JSON on stdin (falls back to a stable constant), asks heartbeat.should_auto_resume, and on True stamps the dedup (heartbeat.mark_resumed) + writes hookSpecificOutput.additionalContext instructing the session to re-run /auto-maintainer:start to re-arm the in-session heartbeat — at most once per session. On False (no intent / latched STOPPED|ABORTED / owed RESTART / already armed) it stays silent. It NEVER schedules a heartbeat itself (a hook cannot dispatch the Agent tool) and NEVER breaks the session (any error -> silent, exit 0). The decision logic lives in heartbeat.py; this hook is the thin SessionStart wiring. Registered as a SECOND SessionStart command in the shipped hooks.json (packaging-config plugin_assets), alongside session-start-persona.py; build_plugin ships heartbeat.py into lib/ and the hook resolves it from ../lib"
    ]
  },
  "reads": {
    "files": [
      "durable state JSON file (path injected via run_tick state_path / TickContext 'state_path' slot)",
      "journal JSONL file (path injected via run_tick journal_path / TickContext 'journal_path' slot)",
      "disposition + lock markers (under the injected runtime_dir)",
      "project-local ${project_dir}/.auto-maintainer/route.json and adapter-map.json (override config; loaded + validated by adapter-wiring, default used when absent)",
      "project-local ${project_dir}/.auto-maintainer/config.json (central config; loaded by safety-governance load_config — migrated once from a legacy governance.json — documented defaults used when absent)",
      "durable budget window {window_key, spent_tokens} under the durable-state BUDGET_KEY (a durable cross-tick fact)",
      "durable acted-ledger {work_order_id: {outcome, ref}} under the durable-state ACTED_LEDGER_KEY (a durable cross-tick fact; idempotency for ACTING agent-states)",
      "shipped plugin manifest <lib_dir>/../.claude-plugin/plugin.json (status.py reads its `version` for the status report's plugin_version; the installed-plugin deployment context, not a feature artifact; null when absent, e.g. the source tree)"
    ],
    "external": [
      "fsm-contracts: TickContext, StateResult, StateManifest, SignalVocabulary, apply_result",
      "tick-orchestrator: run, RunResult",
      "durable-state: DRAIN/PERSIST states, DRAIN/PERSIST manifests, DurableState, Journal, SCHEMA_VERSION",
      "lifecycle-dispositions: Guard, Exit, Disposition, read_disposition/write_disposition, lock helpers",
      "work-intake: Pull (constructed with the work_own_filings flag make_pull threads from the loaded config, §3.11.5 — default True includes loop-filed items, false excludes them at PULL — AND the issue_filter object make_pull threads from the loaded config: DNF labels + title_pattern narrowing pulled issues, default no-filter pulls all open), PULL_MANIFEST, Triage, TRIAGE_MANIFEST, WORK_ITEMS_SLOT, WORK_ORDERS_SLOT, CROSS_CUTTING_RISK_SLOT (TRIAGE writes it, VERIFY reads it — FT-B/D §3.5.9), DiscoveredIssue, file_discoveries, gh_issue_file_sink, gh_issue_source, parse_gh_issues, reject_dispositions(work_orders) + gh_issue_reject_sink(issue_ref, repo, reason) + REJECTED_LABEL (the Wave-2 reject-disposition enactment at the TRIAGE resume — comment + label, never close, trust-gated by permits('file', mode))",
      "prioritize: PRIORITIZE_MANIFEST, EXECUTION_PLAN_SLOT, run (PRIORITIZE reads work_orders, writes execution_plan)",
      "implement: IMPLEMENT_MANIFEST, HANDOFFS_SLOT, run (dry-run IMPLEMENT reads execution_plan, writes handoffs; INERT)",
      "verify-integrate: Verify/VERIFY_MANIFEST, REVIEW_MANIFEST/REVIEW_FINDINGS_SLOT/REVIEW_FINDINGS_SCHEMA_VERSION/review_finding_record (the ADVISORY REVIEW state, FT-C), CROSS_CHECK_SLOT (VERIFY's complement-run product, FT-D), Integrate/INTEGRATE_MANIFEST/INTEGRATION_RESULT_SLOT/INTEGRATION_RESULT_SCHEMA_VERSION (thin merge reading ONLY verdicts), Cleanup/CLEANUP_MANIFEST, VERDICTS_SLOT, merge_guardrails, gh_open_pr_source, gh_pr_merge_sink, gh_open_pr_source (the acted-ledger re-entry check mirrors verify-integrate's PR-state seam), and (Wave-2) Reconcile/RECONCILE_MANIFEST/RECONCILE_RESULT_SLOT/ReconcileResult + gh_pr_state_source/gh_issue_state_source/gh_issue_close_sink/gh_pr_close_sink/gh_issue_comment_sink/reconcile_rebase_worktree/gh_default_branch_source — wrapped UNCHANGED by make_verify/make_review/make_integrate/make_cleanup/make_reconcile so VERIFY/REVIEW/INTEGRATE/CLEANUP/RECONCILE wire by a pure route.json edit (close-the-loop; RECONCILE runs before PULL)",
      "adapter-wiring: build_loop, resolve_states, load_route, load_adapter_map, validate_wiring, WiringError (load + resolve + validate route-as-data against DEFAULT_ROUTE/DEFAULT_ADAPTER_MAP; resolve_states/build_loop also used by route_config.py + adapter_map_config.py to VALIDATE a user edit before writing route.json/adapter-map.json — adapter-wiring stays the dependency-free validator the CLIs call)",
      "safety-governance: load_config (alias load_governance), work_own_filings(config) (the §3.11.5 loopback toggle make_pull binds onto work-intake's Pull, default True), issue_filter(config) (the pure DNF-label + title_pattern normalizer make_pull binds onto work-intake's Pull, default no-filter), issue_filter_apply_labels(config) (the first AND-group of issue_filter labels; the REPORT flush passes it as file_discoveries' apply_labels so project-target loop-filed issues carry the labels a later PULL filters on), evaluate_budget, record_spend(budget_state, now, tokens), permits(effect_kind, mode) (the trust-ladder gate over the closed effect set {implement, open_pr, merge, file}); the fixed MAINTAINER_REPO constant (the maintainer-self REPORT destination consumed by run_tick._repo_for_target — no project fallback, §3.11.6); and the central config fields config.backoff.threshold (default 5, the backoff defer count) + config.heartbeat.interval_minutes (default 3, the /start heartbeat cadence). Consumed to trust-gate ACTING agent-states, budget-pre-gate via evaluate_budget, meter spend via record_spend, route maintainer-self discoveries, gate backoff, and schedule the heartbeat",
      "agent-dispatch: build_envelopes(adapter, slot_values, tick_context, state, output_dir), render (its ## Handoff embeds output_path + schema + write-to-file ack), validate_output(file_content, schema), collect_outputs, compute_signal (consumed UNCHANGED — the deterministic helpers around an in-session agent dispatch; run_tick emits the dispatch request, reads the subagent-WRITTEN output files at output_path on resume, and applies the results, never dispatching itself)",
      "observability: EventLog (append/read/tail) + EVENT_KINDS (consumed UNCHANGED — run_tick opens an EventLog at ${runtime_dir}/events.jsonl and appends one structured event per tick milestone; ts comes only from the injected now, seq from the lib's line count)"
    ]
  },
  "invokes": {
    "scripts": ["src/run_tick.py (--step/--resume, driven once per tick by the /auto-maintainer:tick executor, which the /auto-maintainer:start prompt-heartbeat fires each interval)", "src/start.py --clear-only (the FRESH-start latch decision, invoked by /auto-maintainer:start before tick #1) + start.py emits config.heartbeat.interval_minutes (default 3) for the /start heartbeat schedule"],
    "external": ["adapter-wiring: build_loop(default_route, default_map, runtime, start, initial) -> (route, states)"],
    "agents": []
  },
  "never": [
    "edits or forks fsm-contracts, tick-orchestrator, durable-state, lifecycle-dispositions, work-intake, prioritize, implement, verify-integrate, adapter-wiring, safety-governance, agent-dispatch, or observability (consumed unchanged)",
    "calls the Agent tool / any model / subprocess from run_tick (the yield/resume seam only EMITS dispatch requests and applies provided results; the executor that performs the Agent dispatch is a later slice)",
    "budget-pre-gates / acted-ledger-filters / spend-meters a NON-acting agent-state (TRIAGE) or a pure-script route (those are unchanged; the doer governance is acting-agent-state-only)",
    "computes a budget allowance / window rollover / spend accrual itself (owned by safety-governance: evaluate_budget + record_spend; scheduling only calls them + persists the returned budget_state)",
    "re-implements the run loop / transition resolution (owned by tick-orchestrator)",
    "re-implements DRAIN/PERSIST/journal/durable persistence (owned by durable-state)",
    "re-implements GUARD/EXIT/disposition/mutex (owned by lifecycle-dispositions)",
    "re-implements route load/resolve/validate (owned by adapter-wiring; scheduling only supplies the default route + the built-in factories)",
    "owns / defines the tick-interval config field (the interval is config-driven via safety-governance's config.heartbeat.interval_minutes, default 3 — #17 RESOLVED; scheduling only READS it via sg.load_config and schedules the /start heartbeat at that cadence, never defining the config schema itself)",
    "assembles or copies the plugin tree (owned by packaging-config, which collects ship/)"
  ]
}
```
