{
  "name": "packaging-config",
  "version": "0.7.34",
  "owner": "changyu87",
  "paths": [],
  "tdd_state": "test-green",
  "created": "2026-06-11T03:26:28Z",
  "deprecation_criterion": "Superseded when the framework adopts a different distribution channel than a self-hosted Claude Code plugin marketplace, or when later slices fold this into a full configure/run UX feature.",
  "updated": "2026-08-16",
  "spec_change_reason": "Cut release v0.37.0 (minor, ASSET-REFRESH \u2014 merge-signal observability wave): bump _PLUGIN_VERSION 0.36.0 -> 0.37.0 and regenerate the committed plugin tree so the shipped lib/status.py (re-normalized to carry scheduling 0.52.0's injectable open-loop-PR probe DEFAULT_OPEN_PR_SOURCE: status_data().open_loop_prs surfaces [{number, auto_merge_enabled, merge_state}] and render_status shows each loop PR's auto-merge posture pending/off + merge_state, or 'open loop PRs: none') and the shipped skills/{start,tick}/SKILL.md (re-collected from ship/, now start v0.6.0 + tick v0.9.0) carry the merge-signal interpretation guidance: merged counts in-tick merges only; merged=0 with auto_merge_enabled>0 is pending async auto-merge, NOT blocked; real blocks are integrate_errored/reconcile_errors; async completions surface as RECONCILE auto_merged. No _LIBS change (status.py already registered in _NORMALIZED_LIBS) and no shipped default-config CONTENT change (schema unchanged); run_tick.py/work_intake.py byte-identical to 0.36.0; no shipped-agent change. The version-assertion test advanced to 0.37.0, an e2e test added asserting the committed + freshly built lib/status.py carries the open-loop-PR probe and the shipped start/tick skills carry the merge-signal guidance, the two skill-version assertions advanced (start 0.5.0 -> 0.6.0, tick 0.8.0 -> 0.9.0), test/release_lib_baseline.json re-anchored (version -> 0.37.0, fresh lib_digest), and the housekeep doc baseline re-anchored to the grown spec (506 -> 521) so the drift, #355 monotonicity, and doc-size guards stay green.",
  "spec_no_change_reason": "Spec already updated with the Plugin minor v0.37.0 section in an earlier commit; this cycle carries the asset-refresh build regen (_PLUGIN_VERSION bump + tree regenerate) and test/baseline re-anchor only."
}
