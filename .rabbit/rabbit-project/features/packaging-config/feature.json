{
  "name": "packaging-config",
  "version": "0.7.17",
  "owner": "changyu87",
  "paths": [],
  "tdd_state": "test-green",
  "created": "2026-06-11T03:26:28Z",
  "deprecation_criterion": "Superseded when the framework adopts a different distribution channel than a self-hosted Claude Code plugin marketplace, or when later slices fold this into a full configure/run UX feature.",
  "updated": "2026-07-28",
  "spec_change_reason": "Cut release v0.25.0: bump _PLUGIN_VERSION 0.24.0 -> 0.25.0 (the single-source-of-truth release constant; #355 monotonicity requires strictly-greater) and regenerate the committed plugin tree so the shipped libs carry the merged verify-integrate 0.12.0 + scheduling 0.40.0 RECONCILE/auto-merge/dedup fixes. The regenerated tree updates only the version stamps (plugin.json + marketplace.json -> 0.25.0) and the naturally-changed lib/verify_integrate.py + lib/run_tick.py bytes; every other shipped byte is unchanged. The version-assertion test advanced to 0.25.0 and test/release_lib_baseline.json re-anchored (version + lib_digest) so the drift + #355 monotonicity guards stay green.",
  "spec_no_change_reason": "release rebuild; version is the build_plugin.py _PLUGIN_VERSION constant, no spec surface change"
}
