{
  "name": "packaging-config",
  "version": "0.7.29",
  "owner": "changyu87",
  "paths": [],
  "tdd_state": "test-red",
  "created": "2026-06-11T03:26:28Z",
  "deprecation_criterion": "Superseded when the framework adopts a different distribution channel than a self-hosted Claude Code plugin marketplace, or when later slices fold this into a full configure/run UX feature.",
  "updated": "2026-08-03",
  "spec_change_reason": "Cut release v0.33.0 (minor, LIB-REFRESH \u2014 loop-convergence wave): bump _PLUGIN_VERSION 0.32.0 -> 0.33.0 and regenerate the committed plugin tree so it carries work-intake 0.12.0 + implement 0.12.0 + scheduling 0.47.0's sources \u2014 lib/work_intake.py (new pure is_in_flight + Pull(in_flight_issue_refs): an UNCONDITIONAL PULL exclusion of any issue that already has an OPEN loop PR), lib/implement.py (Handoff schema 1.1.0->1.2.0 adds the terminal already_done status carrying already-on-main evidence), lib/run_tick.py (make_pull computes the in-flight issue-ref set from acted-ledger opened entries live-confirmed OPEN and threads it into Pull; an already_done handoff records a terminal-resolved triage_memory skip without incrementing backoff), and agents/auto-maintainer-implementer.md (v2.12.0). No _LIBS change and no shipped default-config CONTENT change (still schema 2.9.0, same values). The version-assertion test advanced to 0.33.0, an e2e test added asserting the committed libs + agent carry the v0.33.0 in-flight-PULL-exclusion + already_done (schema 1.2.0) + in-flight-wiring tokens, test/release_lib_baseline.json re-anchored (version -> 0.33.0, lib_digest -> the new committed-lib digest), and the housekeep doc baseline re-anchored to the grown spec (427 -> 446) so the drift, #355 monotonicity, and doc-size guards stay green.",
  "spec_no_change_reason": "spec.md already updated to 0.7.23 (v0.33.0 release note) in preceding commit d159dcb; this cycle implements the _PLUGIN_VERSION bump + committed-tree regen"
}
