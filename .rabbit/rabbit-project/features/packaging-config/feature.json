{
  "name": "packaging-config",
  "version": "0.7.21",
  "owner": "changyu87",
  "paths": [],
  "tdd_state": "test-green",
  "created": "2026-06-11T03:26:28Z",
  "deprecation_criterion": "Superseded when the framework adopts a different distribution channel than a self-hosted Claude Code plugin marketplace, or when later slices fold this into a full configure/run UX feature.",
  "updated": "2026-07-29",
  "spec_change_reason": "Cut release v0.25.4: bump _PLUGIN_VERSION 0.25.3 -> 0.25.4 (the single-source-of-truth release constant; #355 monotonicity requires strictly-greater) and regenerate the committed plugin tree so the shipped agents/auto-maintainer-reviewer.md carries the merged verify-integrate 0.12.3 change (the REVIEW reviewer is scoped to the PR's own diff \u2014 it no longer files merge-conflict/collision findings). The regenerated tree updates only the version stamps (plugin.json + marketplace.json -> 0.25.4) and the naturally-changed agents/auto-maintainer-reviewer.md bytes; the shipped lib/ is byte-identical (its digest is unchanged), so every other shipped byte is unchanged. The version-assertion test advanced to 0.25.4 and test/release_lib_baseline.json re-anchored (version -> 0.25.4; lib_digest unchanged) so the drift + #355 monotonicity guards stay green.",
  "spec_no_change_reason": "release rebuild; version is the build_plugin.py _PLUGIN_VERSION constant, no spec surface change"
}
