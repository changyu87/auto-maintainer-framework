{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces, re-baselined for the #255 evidence-gated REVIEW feature wave, which adds the REQUIRED ReviewVerdict.evidence schema field and the deterministic evidence-gated trust backstop (review_evidence_valid / batch_is_untrustworthy) — load-bearing schema/behavior content, not bloat. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "fb2c930",
  "docs": {
    "docs/spec.md": 158,
    "docs/contract.md": 46
  }
}
