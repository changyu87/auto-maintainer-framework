{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces, re-anchored after the cumulative GATE feature wave (DESIGN §2.2 [v2]) legitimately extended docs/spec.md with load-bearing content: the GATE slot-contract row, the GateResult schema, gate_failed on IntegrationResult, and the GATE section (no-op when regression_command null; else cumulative integration-worktree with per-PR merge+regression, roll-back-on-fail, exclude, deterministic order). The prior measured doc-reduction anchor (spec.md 201) is superseded by this load-bearing extension per this fixture's own re-capture criterion. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 257,
    "docs/contract.md": 51
  }
}
