{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces. Re-anchored after the RECONCILE tier-1 unique-worktree-robustness wave legitimately extended the doc surface with load-bearing content: docs/spec.md gained the `Worktree-path robustness (unique per invocation)` paragraph in the tier-1 ladder + the matching `reconcile_rebase_worktree uses a UNIQUE per-invocation worktree path` invariant (tempfile.mkdtemp, git worktree prune before add, so a tick killed mid-rebase never wedges later tier-1 attempts on a leftover worktree), growing spec.md from 665 to 685; docs/contract.md is unchanged at 57. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 685,
    "docs/contract.md": 57
  }
}
