{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces. Re-anchored after the RECONCILE prior-verdict race-breaker wave legitimately extended the doc surface with load-bearing content: docs/spec.md gained the OPTIONAL prior_verdicts input description + the LIVE-PREFERRED (B)-ladder conflict-determination paragraph + the matching invariant (a settled live mergeable is authoritative; ONLY a live UNKNOWN consults a CONFIRMED-CONFLICTING prior verdict; absent/empty ⇒ today's behavior), growing spec.md from 569 to 616; docs/contract.md gained the prior_verdicts reads entry + the (B) live-preferred clause, growing from 56 to 57. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 616,
    "docs/contract.md": 57
  }
}
