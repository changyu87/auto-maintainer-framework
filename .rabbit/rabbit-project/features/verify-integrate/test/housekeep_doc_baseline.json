{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces. Re-anchored after the disable-repo-hooks + gh-graphql-closing-issue wave legitimately extended the doc surface with load-bearing content: docs/spec.md gained the `Hooks-free disposable-tree git ops` tier-1 paragraph + the `Disposable-tree git ops run hooks-free` invariant, and rewrote the gh_closing_issue_ref resolver descriptions (dedup source, dedup C, invariant) to the `gh api graphql` + Closes/Fixes/Resolves #N body-parse form, growing spec.md from 685 to 718; docs/contract.md is unchanged at 57. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 718,
    "docs/contract.md": 57
  }
}
