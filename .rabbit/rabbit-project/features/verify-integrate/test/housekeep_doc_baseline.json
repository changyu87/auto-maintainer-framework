{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces. Re-anchored after the INTEGRATE auto-merge wave legitimately extended docs/spec.md with load-bearing content: the INTEGRATE 'merge via GitHub auto-merge' bullet (gh pr merge --auto --merge --delete-branch, immediate-fallback when the repo has auto-merge disabled, the new IntegrationResult.auto_merge_enabled pending-success outcome, INTEGRATION_RESULT_SCHEMA_VERSION additive bump), which set spec.md to 347. contract.md is unchanged at 52. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 347,
    "docs/contract.md": 52
  }
}
