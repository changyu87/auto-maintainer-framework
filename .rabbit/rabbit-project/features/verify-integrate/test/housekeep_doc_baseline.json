{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces. Re-anchored after the RECONCILE tier-1 URL-ref-crash-fix wave legitimately extended the doc surface with load-bearing content: docs/spec.md gained the `pr_ref` form robustness paragraph in the tier-1 ladder + the matching `_pr_number(pr_ref)` dual-form-tolerance invariant (parses BOTH owner/repo#N and a full GitHub PR URL, raising ValueError only when no number is parseable, so reconcile_rebase_worktree never crashes on a URL-form ref), growing spec.md from 647 to 665; docs/contract.md is unchanged at 57. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 665,
    "docs/contract.md": 57
  }
}
