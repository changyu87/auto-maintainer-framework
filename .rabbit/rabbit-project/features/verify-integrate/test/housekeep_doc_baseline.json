{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces. Re-anchored after the v0.25.3 RECONCILE gh_pr_state_source hotfix legitimately extended the doc surface with load-bearing content: docs/spec.md gained the gh_pr_state_source transient-mergeable=UNKNOWN bounded-poll seam clarification (it resolves UNKNOWN via the same poll_mergeability VERIFY uses; a MERGED PR short-circuits, a still-UNKNOWN result defers) + the extended RECONCILE transient-UNKNOWN convergence invariant, growing spec.md from 537 to 553; docs/contract.md is unchanged at 56. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 553,
    "docs/contract.md": 56
  }
}
