{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces. Re-anchored after the MERGE-QUEUE-AWARE INTEGRATE wave legitimately extended docs/spec.md with load-bearing content: the INTEGRATE merge bullet now describes the merge-queue detection (GraphQL repository.mergeQueue(branch)), the queue-present `gh pr merge <pr> --auto` path (no method flag, no --delete-branch), the no-queue immediate `--merge --delete-branch` -> not-yet-mergeable `--auto --merge` fallback, and gh stderr capture into IntegrationResult.errors, which set spec.md to 355. contract.md is unchanged at 52. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 355,
    "docs/contract.md": 52
  }
}
