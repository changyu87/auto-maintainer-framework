{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces, re-baselined for the FT-D loop-redesign wave (VERIFY becomes thin with CI optional + the conditional cross-feature complement run), which adds the load-bearing CrossCheck slot schema + the §3.7.6 complement-run behavior contract — load-bearing schema/behavior content, not bloat. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 194,
    "docs/contract.md": 51
  }
}
