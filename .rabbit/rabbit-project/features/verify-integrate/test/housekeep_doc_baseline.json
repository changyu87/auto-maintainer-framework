{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces. Re-anchored after the orphaned-loop-PR convergence wave legitimately extended docs/spec.md with load-bearing content: the Verdict `orphaned` field + the INTEGRATE `closed_orphaned` field in the schemas block, the VERIFY orphaned-PR-detection bullet (closing-issue-state resolution via gh_closing_issue_state, forced ok=False, conservative-on-uncertainty), and the INTEGRATE close-orphaned bullet (gh_pr_close_sink, trust-gated on permits('merge', mode), would-close-under-skipped at propose/dry-run, fault->errors), which set spec.md to 323. contract.md is unchanged at 52. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 323,
    "docs/contract.md": 52
  }
}
