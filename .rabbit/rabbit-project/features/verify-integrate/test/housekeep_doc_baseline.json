{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces, re-anchored after the doc-surface load-bearing-token survival GATE wave (issue #353) legitimately extended docs/spec.md + docs/contract.md with load-bearing content: the new 'load-bearing' GateResult reason, the doc-survival bullet in the GATE section (declared tokens in test/load_bearing_tokens.json checked against the merged worktree's doc surfaces before the regression; roll-back-on-drop; opt-in + doc-scoped), and the contract provides entry for declared_load_bearing_tokens/missing_load_bearing_tokens/features_with_changed_doc_surfaces. The prior anchor (spec.md 257, contract.md 51 from the cumulative GATE wave, DESIGN §2.2 [v2]) is superseded by this load-bearing extension per this fixture's own re-capture criterion. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 274,
    "docs/contract.md": 52
  }
}
