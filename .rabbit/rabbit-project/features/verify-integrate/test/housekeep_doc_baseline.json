{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next verify-integrate housekeep wave's baseline; single-use measured-reduction reference, re-captured whenever a feature wave legitimately extends the doc surface with load-bearing content.",
  "description": "Line counts of verify-integrate doc surfaces. Re-anchored after the v0.25.1 RECONCILE (C) dedup hotfix legitimately extended the doc surface with load-bearing content: docs/spec.md gained the gh_open_pr_closing_issue_source list-then-per-PR-view clarification (closingIssuesReferences is a gh pr view-only field, never valid on gh pr list) + the whole-step dedup fault-isolation paragraph + the extended (C) convergence invariant, growing spec.md from 515 to 537; docs/contract.md is unchanged at 56. The housekeep e2e test asserts the current docs are NOT LARGER than these baselines (a reduced|no-op verdict) and that the load-bearing tokens survived.",
  "baseline_commit": "HEAD",
  "docs": {
    "docs/spec.md": 537,
    "docs/contract.md": 56
  }
}
