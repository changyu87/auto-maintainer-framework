{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next work-intake housekeep wave's baseline; a re-bloat regression guard for the post-#359 doc surfaces.",
  "description": "Re-anchored for the apply_labels spec change: the filing sink's REPORT bullet additively documents that gh_issue_file_sink stamps the PULL-visibility apply_labels (ensuring each exists first) alongside the provenance label, and file_discoveries forwards them only for project-target discoveries, a legitimate new behaviour, not re-bloat. This baseline records the post-apply_labels doc line counts plus a one-line margin, so the housekeep gate continues as a re-bloat regression guard (current docs must stay STRICTLY smaller than this) while the load-bearing-token + cleanup gates remain intact.",
  "baseline_commit": "post-apply-labels",
  "docs": {
    "docs/spec.md": 326,
    "docs/contract.md": 49
  }
}
