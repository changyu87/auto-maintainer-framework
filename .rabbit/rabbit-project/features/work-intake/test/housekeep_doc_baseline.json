{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next work-intake housekeep wave's baseline; a re-bloat regression guard for the post-#359 doc surfaces.",
  "description": "Re-anchored for the park-distinct-attempts spec change: spec.md additively documents that is_parked counts DISTINCT failed PRs (parsed pr_ref from each gate-fail marker's JSON payload) rather than raw GATE_FAIL_MARKER occurrences, making park a true retry counter — a legitimate new behaviour, not re-bloat. This baseline records the post-park-distinct doc line counts plus a one-line margin, so the housekeep gate continues as a re-bloat regression guard (current docs must stay STRICTLY smaller than this) while the load-bearing-token + cleanup gates remain intact.",
  "baseline_commit": "post-park-distinct",
  "docs": {
    "docs/spec.md": 270,
    "docs/contract.md": 48
  }
}
