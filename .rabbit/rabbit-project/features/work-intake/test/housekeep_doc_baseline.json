{
  "schema_version": "1.0.0",
  "owner": "changyu87",
  "deprecation_criterion": "Superseded by the next work-intake housekeep wave's baseline; a re-bloat regression guard for the post-#359 doc surfaces.",
  "description": "Re-anchored for the Phase 2 park-guard spec change: spec.md additively documents the UNCONDITIONAL PULL park exclusion (is_parked / PARK_THRESHOLD), a legitimate new behaviour, not re-bloat. This baseline records the post-park-guard doc line counts plus a one-line margin, so the housekeep gate continues as a re-bloat regression guard (current docs must stay STRICTLY smaller than this) while the load-bearing-token + cleanup gates remain intact.",
  "baseline_commit": "post-park-guard",
  "docs": {
    "docs/spec.md": 259,
    "docs/contract.md": 48
  }
}
