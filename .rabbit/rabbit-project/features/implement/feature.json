{
  "name": "implement",
  "version": "0.6.1",
  "owner": "changyu87",
  "paths": [
    "rabbit-project/features/implement/src/**"
  ],
  "created": "2026-06-12T06:10:37Z",
  "deprecation_criterion": "Superseded when the model-backed implement-then-PR doer (DESIGN \u00a73.6.2/\u00a73.6.3) replaces the dry-run reference adapter, or when the Handoff schema reaches a breaking major version.",
  "tdd_state": "test-green",
  "updated": "2026-06-23",
  "spec_change_reason": "Deployment-correctness fix: the shipped implementer subagent (ship/agents/auto-maintainer-implementer.md) invoked the gate via a DEV source-tree path (python3 <repo>/rabbit-project/features/implement/src/test_gate.py ...), which is non-functional in the installed plugin and leaks the dev layout. Change the gate-invocation example to the deployed Claude Code convention ${CLAUDE_PLUGIN_ROOT}/lib/test_gate.py (mirroring scheduling's start/adapter-map shipped skills); the shipped body now references NEITHER 'rabbit-project' NOR '.rabbit'. spec.md documents the gate shipping to plugin lib and the deployed invocation path; e2e tests assert the deployed path and the absence of any source-tree leak. Accept-path semantics unchanged.",
  "spec_no_change_reason": "DOC-dimension measured-reduction housekeep wave: coding-rules \u00a76 verify-or-flag pass applied to docs/spec.md + docs/contract.md. Every path ref (ship/agents/auto-maintainer-implementer.md), cross-feature ref (adapter-wiring, scheduling.run_tick, safety-governance), and behaviour claim verified LIVE via find/grep/ls; no proven-dead content found. Honest verdict: no-op (already-clean), a valid PASS. No spec body change; the wave ships the gating housekeep e2e test + baseline fixture only."
}
