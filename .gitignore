# Rabbit workflow (vendored dev tool, plugin mode) — not part of the framework source

# rabbit vendored install — ignore ONLY ephemerals; track the whole .rabbit/ tool+project (Strategy D, #1086)
.rabbit/.runtime/
